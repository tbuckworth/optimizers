# Component-utility implementation acceptance

Codex — Spectral Optimizer Investigation · 10 September 2026, 19:24 UTC

**Main accepts the fixed producer, independent saved-array auditor and runtime
guard for source freeze and one separately recorded local acquisition/audit.**
All109 fabricated CPU tests pass in3.946s. This is implementation evidence,
not a scientific result; no real checkpoint or scientific array has been
opened for this diagnostic. Current original producer/audit JSON metadata
bindings pass without reading their numeric arrays or replaying a model.

## Review and verification

Main read all three new modules, tests and author notes completely, plus the
fixed protocol/interface and accepted helper APIs. An independent reviewer
read the complete producer and fixtures; final16-test independent suite passes
in1.212s. See [producer review](runner-source-review.md),
[guard author record](guard-implementation.md) and
[independent auditor implementation](audit-implementation.md).
Main is the independent reviewer of the guard/auditor authored by the leaves.

Resolved source-review findings: nonblocking regular-input opens prevent FIFO
blocking; numeric headers are checked before allocation; all reads parse pinned
bytes; guard provenance checks the exact preexisting-client subset and physical
free/total consistency. These are integrity corrections, not altered scientific
choices or outcome-calibrated tolerances. NPZ failure writes use context-managed
ZIP/NPY streams and retain partial files. Every real attempt is exclusive.

The new integration fixture sends actual fabricated CPU canonical raw/native
actions at100/56304 through the independent NumPy Adam/projection/path audit.
It also compares Torch metrics to independent NumPy reductions. A mocked whole
parent flow writes all31 artifacts, and a baseline mismatch is archived and
rejected before any action. The remaining tests cover archive/hash/shape caps,
clock/ownership/immutability, six objective gradients, exact25-view input
ordering, rounded paths, decay accounting, all-cell summaries and tiny/empty
subsets. Earlier four accepted modules and every old scientific dependency
remain unchanged.

```text
CUDA_VISIBLE_DEVICES= OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 python3 -m unittest discover -s tests -p 'test_spectral_component_utility*.py'
Ran 109 tests in 3.946s
OK
```

Source SHA256:

```text
experiments/spectral_component_utility.py
9078630f867257d716027c670d6382119ed49b7f807073c8c107e4dc57534ffe
experiments/spectral_component_utility_audit.py
9e938a3fd2720a95124a422a18a6470f114d0b7a0b64d641c7f6567dc35cf897
experiments/spectral_component_utility_guard.py
77e2f00fa113a92592cfd9f543ce4a9ab730cc097b44c60c69fd2b24f87e5bce
tests/test_spectral_component_utility.py
1c05e9a869289de7afb3ba1e9f7f5aaf03700d392427fe878d81b7f5927cb635
tests/test_spectral_component_utility_audit.py
877a056b68c7a9c1c5c4c20cf2e7f8deb1051853701994b062fc38de6548e94e
tests/test_spectral_component_utility_guard.py
c51613b5a2887b9767cad178e4a3e6dca3013d5682a1636884e44285c87891d4
```

The final archive-contract addition records the already agreed manifest,
attempt/provenance schema and exact implemented byte allowance. It changes
neither mathematical choices nor producer/reader behavior. The framework-check
addendum qualifies allocator limits and numeric-reader responsibilities.

## Concrete storage and object-lifetime review

`byte_inventory()` permits6,413,406,496 bytes plus1,048,576 failure reserve,
below8,589,934,592. The dominant24 native post-bases occupy at most4,514,803,200
bytes. Each largest numeric artifact is below256MiB. Panels, JSON and all ZIP
headers fit within an explicit256MiB overhead allowance; the writer also checks
actual bytes on every write. Final inventory is376 receipts plus `results.json`.
The hard archive cap is enforced independently of favorable prospective sums.

Only one restored parent is processed at a time. Its CPU source dictionary is
discarded after restoration and exact round-trip hashing. Baseline graph objects
are scoped to a returning function; only detached logits/gradients escape.
Raw/native branches are copied sequentially. Post-bases are archived and released
before endpoint readouts, retaining only three candidate vectors. Each readout
uses one private model under no-grad; results retain JSON/receipts, not models.
The fixed input grid is9,600 images×784 FP32 (30,105,600 bytes), plus original,
baseline and two small action inputs. Each parameter/moment vector is940,584
bytes. Streaming low-rank updates do not allocate P×P covariance. Kernel/BLAS
workspaces and allocator caching still make an exact prospective process peak
unavailable:8GiB host/no-swap and4GiB PyTorch GPU allocator limits are enforced,
not asserted measured peaks. Failure is retained rather than shrinking the run.

Audit processes one numeric artifact/basis at a time. The largest FP32 Q has
188,116,800 bytes and its temporary FP64 conversion376,233,600 bytes. Reader
payload bytes are released before its projection calculation; no12-parent
basis accumulation occurs. Scalar summaries, small logits and four action
vector sets per parent remain bounded. The2GiB host/no-swap limit is enforced.

## Next admitted scope, not an automatic retry

Commit this reviewed implementation, then record the exact source manifest,
fresh hardware/mount check, unique directory and fixed units in `admission.md`.
The prospective hardware check shows /dev/RECONFIGURE_FOR_LOCAL_STORAGE at/private-artifacts/storage,144.1G free,
RTX3090 with23587MiB free and only known clients2101/260MiB and8861/27MiB.
This is not a reservation; launch rechecks it. Old strong producer/audit units
are inactive/dead with MainPID0; the user reminder remains active/waiting.

No new user approval is required within the existing autonomous scope. Run
only all12 registered parents,24 draws and144 endpoints, with one CPU,
8GiB host/no swap,4GiB GPU allocator,1200-second cooperative/1800-second hard
limits, no restart, and8GiB archive. Then perform one900/1200-second,2GiB
CPU-only audit with at most64MiB output outside the acquisition. No real-data
smoke test, source changes after freeze, retry, model training continuation,
roster shrink, paid compute or broader capability benchmark is selected.

On audited success, interpret all cells and send the reviewed executive HTML
report with plots. A failure is an integrity/runtime outcome, not scientific
evidence about the filter. The broad goal and two-hour timer remain active.
