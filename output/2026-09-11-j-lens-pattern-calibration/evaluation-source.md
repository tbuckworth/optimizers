# Isolated evaluation producer — source-only freeze

No actual evaluation stage has run. Collection, whole-panel tokenizer preflight and calibration are consumed and are not invoked here. Root owns admission, one eventual 32-state measurement and the later committed-response gate before any key inspection.

Sources: [evaluate.py](evaluate.py), SHA256 `0393c1d0683dd43917282e65ead47f2b532efb62e0f4b2d7f7c6928132252e30`; [test_evaluate.py](test_evaluate.py), SHA256 `e5d6d187881b7acb98f8e3941981dfcc69e6c2311b1f60ed2144f224485c662f`. Frozen main protocol (artifact not distributed in this public snapshot) SHA256 `4d80a9eef24e32a475c1d5016f57ca05419cacb45f2b27504f9ca7f916fae762` remains unchanged.

The normal hook initially flagged only `EVALUATION_TOKENS_SHA`, a committed scientific token-record checksum independently verified against its exact file. Its single-line explanatory `gitleaks:allow` comment is the only producer change from reviewed/tested SHA256 `a01421ead14a43cefe26e2d571cbdd74313e9004aa2357058bbdc3202597b43b`. No algorithm, raw artifact, global rule or ignore list changed; no hook was bypassed.

Reviewed in full: pinned `calibrate.py` (79b1d1a0…), `preflight.py` (05ce0a8a…), original natural-add-on `forward.py` (23c9c7cf…), runtime helper (679834b6…) and upstream `jlens/hf.py` (228cf078…). Only inert/pure helpers are reused: safe import/JSON/hash/NPZ writing/direction loading, metadata/runtime validation, frozen token-record reconstruction, cached model loading, unmodified post-block capture, frozen parameter checks and scalar export. No old entrypoint, lens checkpoint load, decoder, pattern fit or tokenizer call. All 26 prospective input/cache/source hashes were independently checked read-only; no archive was parsed and the actual `evaluation/` path remained absent.

The producer checks that commit `ef8168b6ba441c5e3a594df66d872b159387f2f1` is an ancestor and that its exact three reference/receipt Git blobs match the working files and frozen hashes, before loading the original direction archive/model. Calibration receipt `d4ab847e8239093536f2124e40507cb2b2f56a431aeb2eb1006278a7532cbf15` and references `42508c7272e108a8103596eeedadf836cc7803475cd64918c3e6fb1e24ce0296` are immutable. No calibration feature, pattern, decoder array or calibration-row file is read. The whole-preflight receipt supplies metadata bindings without opening its calibration token file.

Only T01-L/R through T16-L/R are measured, with exact 16-word strings, complete masks/offsets and final actual subtoken at layer 11. The 32-row/16-pair ordering, fixed manifest evaluation roles and source bindings are checked. The same BF16/eager/eval/frozen/no-grad model and current Aug31 Python/package/binary pins apply. Scores are `(float64(h32)-source_mean64) @ float64(u32)`; authoritative gaps subtract these saved L/R scores, not a separately rounded difference projection. There is no centering/refitting/normalization change.

New exclusive `evaluation/` contains six files:

- `attempt.json` before any helper/input read; failures retain `failure.json` and never retry.
- `inputs.json`: schema `jlens_pattern_calibration_evaluation_inputs_v1`, role, records, exact pairs, capture location/mask convention.
- `features.npz`: `activation_11` float32 (32,1,1024), `scores64` float64 (32,1,4), `gaps64` float64 (16,1,4), `locations=['prefix_end']`, exact `u32` and `source_mean64`.
- `scores.json`: schema `jlens_pattern_calibration_evaluation_scores_v1`, `locations.prefix_end` list of four `{axis,values:{T-ID:scalar}}` rows.
- `gaps.json`: schema `jlens_pattern_calibration_evaluation_gaps_v1`, locations, axes, fixed orientation and all 16 pair records with singleton-location `gaps64` lists.
- `receipt.json`: schema `jlens_pattern_calibration_evaluation_receipt_v1`, four output hashes/sizes, original reference commit, input/source/runtime/cache pins, counts, frozen/no-fit/no-decode flags, elapsed time and allocator peaks. It contains no score/gap/sign summaries. Failed receipts omit exception values to avoid leaking a scientific key.

Admission requires `JLENS_PATTERN_EVALUATION_GPU_RELEASE=1`, exact `--source-sha`, three thread limits and offline/implicit-token-disabled flags. Root must separately enforce CPUQuota100%, 8 GiB host/no swap, 600 seconds and current resource headroom. The inherited 8 GiB PyTorch allocator bound is not a whole-process GPU cap. The producer deliberately performs measurement before reader locking but does not print its keys; root/agents must keep scientific outputs unopened until all 256 responses and validated locks are committed. This source task grants no actual launch.

Ten fabricated CPU tests PASS, 0.048 seconds after imports, under `/usr/bin/python3`, CUDA hidden, single threads/offline and `timeout 60s`. Fixtures cover inert import, read-set isolation, role/order/word-count/endpoint corruption, exact committed blobs and ancestry failure, full tiny-model hook/FP32 capture, FP64 export, a cancellation construction distinguishing saved-score subtraction from direct delta projection, invalid arrays/model mutation, consumed/dangling stage guards and a complete fabricated metadata-only receipt. All test files/arrays/models are synthetic; no real tokenizer, model weights, source panel, scientific archive or old stage was used.

Best-practices check: explicit gradient disabling matches [PyTorch's no-grad contract](https://docs.pytorch.org/docs/2.11/generated/torch.no_grad.html); eval and parameter freezing remain separate explicit checks. The inherited direction loader uses `allow_pickle=False` and a context manager, as documented by [NumPy](https://numpy.org/doc/stable/reference/generated/numpy.load.html). This validation retained the established implementation; it added no new scientific comparison or runtime change.
