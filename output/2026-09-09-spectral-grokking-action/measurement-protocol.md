# New-state-only action-policy readout

9 September2026. This implements the readout section of the already frozen
[action protocol](protocol.md); it does not change the live training source,
seed roster, actions or horizons. No readout acquisition has started.

Admit the fully completed five-seed batch only:35 new states, native1501 and
orthogonal/norm-matched1501/2000/2500 for every seed100–104. Verify exact ordered
roster, outer intervention identities, inherited model/config/split/source
identities, original parent checkpoint receipts and all completion/state hashes.
Per-seed repository HEADs may differ because of unrelated commits during a live
batch; scientific source hashes must be identical. Preserve all commit IDs as
provenance instead of treating unrelated document commits as scientific drift.

Use the original CUDA/PyTorch/Python/NumPy/GPU readout environment, checked
against accepted original measurement and action-batch manifests. Bind the
original extraction, probe and symmetry source files to their accepted prior
source hashes, not merely their current contents. Keep separate pins for this
new envelope/collection code. Fail before inference on an environment or
original-source mismatch; there is no implicit CPU or changed-recipe fallback.

Prove regenerated split, row-permutation and structural arrays equal the
already archived native1500 arrays for each seed; no old inference is repeated.
Each new state is loaded into a separate model instance and observed exactly
once with the frozen batch1024 extraction. Save logits, final/pre-attention
activations, IDs and null permutations. Fit the existing ridge.001/readout on
the same never-trained fit/evaluation halves, preserve all56 frequencies and
20null selections, and retain the fixed panel[9,33,32,49,11]. Compute CE,
accuracy, correct-class margin, reviewed symmetry and matched training-membership
excess from those same saved arrays. Check available training-grid evaluations
at2000/2500 within the prior fixed tolerances. Step1501 has no such native
historical reference; this is disclosed, not replaced with a new replay.

This stage outputs per-state artifacts and complete receipts only. Primary
paired endpoint interpretation follows the frozen action protocol after an
independent result audit. A one-step selected-frequency change is not circuit
formation; fixed-panel and continuous behavior diagnostics remain alongside it.

Run once in a new exclusive large-volume output with20minute service deadline,
19minute cooperative bound,16GiB/no swap, one CPU math thread and3GiB output
cap plus1GiB free reserve. Check actual effective cgroup bounds before inference.
No launch while the source batch is live/incomplete, no retries of accepted
states, no baseline restart, no paid compute. Any failure preserves partial
output and its exact accepted-state roster for review.
