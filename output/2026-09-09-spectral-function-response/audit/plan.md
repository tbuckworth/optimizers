# Independent saved function-response check — preparation only

Written before this diagnostic runs. Only source and tiny synthetic fixtures
are permitted now; main must review and explicitly authorize a completed-data
audit. No collector or original optimizer source is changed.

The researcher results-auditor instructions supply independent arithmetic and
claim discipline, overridden here by the user's fixed-roster/no-restart scope:
no new seed/split, inference, backward, optimizer step, observer/SVD update,
training trajectory or replay of raw. A PASS concerns saved data, not fresh
replication or general safety.

## Fixed scope

Use all five seeds100–104 and all three NEW states (`trunc`, `projected`, `zero`)
from the exact prospective `diagnostic-001` output. Reuse each bound first-step
bundle and before/raw logits only to verify common-input/copy identities.
Reconstruct t=Q(Qᵀu) in float64 then compare the saved cast action; check t≈ρv,
Q orthogonality, ρ=||Q Qᵀg||/||g|| and all saved action algebra scalars.
Check only the three new Adam state recurrences (m, v and parameters), complete
parameter order/execution flags and before/after identity hashes. Raw is a saved
state identity check, not a rerun or repeated raw moment audit. Explicit zero
still increments Adam's step, transports moments and applies decay.

From the five action-logit arrays (`before`, `raw`, `trunc`, `projected`, `zero`),
independently compute class-centered finite CE/margin/accuracy, uniform-full-grid
sum-class mean projection, total/sum-consistent/within-sum energy, baseline
signed-CE utilities, convex remainders, four before responses, all six ordered
pair contrasts and telescoping residuals in NumPy. The projector uses independent
sum-class masked means, not the producer's accumulation function. No producer
numerical helper is imported. Check every numerical reported leaf and every
descriptive five-seed mean/SE/sign count with the exact fixed roster.

Paired aggregation is independently derived from the saved per-seed scalar
values after those values are separately checked against raw arrays. This
preserves exact saved signs for floating-point-near-zero diagnostic residuals;
it does not demand bitwise equal residual signs across different sum/reduction
orders or reinterpret roundoff signs as learning effects. Squared contrast
energies are not forced to telescope. Utilities use the one shared before-state
negative CE gradient, not a new gradient at either post-state.

## Receipts, bounds and acceptance

Require main-supplied completion SHA256, completed full five-seed/15-state
manifest, original raw batch/measurement pins, every new state/logit/result
receipt and current scientific source hashes. Tie each copied before/raw logit
array and structural IDs to its archived NPZ receipt; tie reused state/action
tensors to the bound inherited bundle. Recheck all input/source hashes at end.

One new five-minute Type=exec audit service: one CPU,16GiB/no swap,280-second
cooperative limit,100MiB output and1GiB reserve. CPU tensor/NumPy computations
only, no GPU or paid usage. Exclusive output
`/tmp/spectral-experiment-artifacts/spectral-grokking-function-response-20260909.PegeVa/audit-001`.
Require the source diagnostic service terminal before real audit input work.
Any failure is preserved; no automatic retry. Main records actual command,
interpreter, PID/invocation and limits before execution.

Use the protocol's numerical acceptance bounds: projection/energy/utility
identities1e−10-scaled, finite telescoping1e−12, CE convex remainder≥−1e−10,
and action/Adam residuals32float32-epsilon with recorded unrounded errors.
Independent per-leaf comparison uses1e−9 relative plus1e−10 absolute for function
metrics, never changes those original acceptance bounds. Tiny CUDA-separated
contrasts remain limited even when algebra is consistent. Neither sum-consistent
energy nor its complement establishes usefulness or memorization, and local
one-step findings do not identify the completed continuation mechanism.
