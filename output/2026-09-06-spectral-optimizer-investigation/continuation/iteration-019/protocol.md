# I19 prospective native stochastic tracking discriminator

Status: SOURCE PREPARATION ONLY, 8 September 2026. No scientific draw, attempt,
bulk root or service is admitted by this draft. I18 is complete and immutable.
Review and freeze source/tests/resource admission before first execution.

## Scientific question and commitments

Does the finite native observer realize any of the directional headroom proved
in the stochastic tracking note (artifact not distributed in this public snapshot), against
strong scalar temporal alternatives, after accounting for finite startup?

The primary practical outcome will be **whole-horizon MSE (1–4000)**, not I18's
late window. Preserve startup1–100, transition101–1000 and late1001–4000 as
fixed secondary mechanism diagnostics. Do not choose the best window, drop
failed seeds, treat rotations as extra seeds or amend arms after outcome reads.
Population direction selection and oracle risk are predictions, not native
guarantees; a native failure despite oracle headroom is informative.

## Streams

- 32 independent seed bundles19000–19031, each with exactly one canonical
  CPU-float64 standard-normal tensor shaped(4000,3), generated by a private
  explicitly seeded torch.Generator. Columns0–1 are measurement noise;
  column2 is process innovation. No other RNG call per seed.
- Measurement epsilon_t = canonical[:,:2] * (1,2). The first latent signal is
  zero. For t>=2 its first coordinate increments by sqrt(Q1)*canonical[t-1,2];
  the unused first process entry is retained, not silently redrawn. Second
  signal coordinate is constantly zero.
- Q1 in (0,.01,.1). These are zero, adverse-population-selection and favorable-
  population-selection cells at beta=.99. All share canonical randomness;
  do not pool them as independent draws.
- Identity and pi/4 joint rotations of signal, measurement noise and oracles;
  192 streams total. Rotation is a paired sensitivity check, not replication.
- 4000 observations per stream, g=s+epsilon. External signals only; no
  optimizer.step, neural forward/backward, GPU, optimizer state checkpoint or
  change to production spectral_filter.py.

## Observer and policies

Unchanged native stable rank-one observer, beta=.99, warmup0, strength1,
normalize/adaptive none, hard, stabletrue, relative tolerance1e−8, absolute
floor0, repair100, dummy double 2-vector SGD wrapper with no optimization.
Save both native action and independent full residual-moment diagnostic as in
I18. Mean mu and first observation initialization follow I18 exactly.

Retain all I18 fixed scalar mixture policies k=0,.5,.9,1 at rho=.9, fixed
uniform EMA .9/.99/.999, raw, useful/nuisance .9/.99 fixed routes, native I17
and native CP at rho=.9. Thus earlier response-state counterevidence remains.

Add three fixed signed controls D_q=2E_q−F_q for q=.9,.99,.999, with E_1=F_1=g_1.
They receive the same scalar observation stream in both coordinates. Negative
older weights and adverse zero/process cases remain visible.

For each nonzero Q1, include an analytical steady-state-optimal common EMA
q_common = 1−(sqrt(Q1²+20Q1)−Q1)/10. Its label must explicitly say theoretical
steady state, not finite-horizon optimum or a learned deployable policy. Omit
it at Q1=0, where no finite q<1 stationary optimum exists.

Because whole-horizon risk includes startup, add two model-based controls
**before source freeze or any scientific draw**. `common_kalman` uses the same
time-varying scalar response in both coordinates. Initialize d_1=g_1,P_1=5.
For t>=2 set q_t=5/(P_(t-1)+Q1+5), d_t=q_t*d_(t-1)+(1-q_t)*g_t,
and P_t=5*(P_(t-1)+Q1)/(P_(t-1)+Q1+5). `useful_oracle_kalman` runs separate
coordinate recurrences with (process,measurement)=(Q1,1),(0,4), each with
P_1 equal to its measurement variance and d_1=g_1, then rotates the output.
Both controls appear in **every** cell, including Q1=0, and use only the
predeclared model parameters and previous state, not realized outcomes or
future observations. Save their response coefficients/posterior variances.
They are explicitly matched-first-observation model-based controls; do not
call them unrestricted finite-horizon optima (for example, all policies are
forced to start at g_1 despite the construction's known initial latent zero).
The common time-varying control is outside the note's fixed-LTI theorem and
must be tested as an actual finite control. The directional version is an
oracle, not an extra learned spectral method.

Steelman the directional response as well: add **one additional native CP**
and one useful-route control with fast decay
rho_star = (2+.1−sqrt(.1²+4*.1))/2 = .7298437881283576,
chosen analytically from the strong cell's Q1=.1,R1=1 **before acquisition**.
Use this same rho_star in every cell, not outcome-dependent or per-cell tuning;
slow mean/complement remains beta=.99. CP_star has d_1=g_1 and
d_t=rho_star*A_t*d_prev+(I−rho_star*A_t)*h_t, with native h=Ag+mu−A*mu.
The useful-route_star is given the true generating axis and separately runs
EMA_rho_star there and EMA_.99 in its complement. Label it an oracle. This
additional response changes no observer state and is not a new production
default. No additional native decay/width/selection sweep is allowed here.

Every policy's first output must equal g_1. The earlier I17 buffer uses its
matching inverse initialization exactly as in I18. Scalar endpoint duplicates
remain explicit (k0=EMA99,k1=EMA9), not extra confirmations.

## Comparisons and interpretation

Primary identity-rotation whole-horizon paired differences for **each of the
two CP responses**, comparator-MSE minus CP-MSE, versus **every other registered
policy**, including raw, the other CP response, I17 and all oracles as well as
uniform/scalar/DEMA/common-Kalman controls. Exclude only a policy's self-contrast.
Keep comparator classes explicit: an oracle contrast is not a comparison to a
learnable method, and a cross-CP comparison is not another scalar control.
Retain the same full matrix in all other windows/rotations and all seed
means/standard errors/sign counts. Define no significance claim
from the number of favorable contrasts; no multiple-testing correction is
implied. Report all registered effects, including contradictory cells.

For strong Q1=.1 the prospective conditional prediction is that the observer
has useful mean squared alignment>.5 in the late window. Whether either CP
beats the registered scalar controls over the whole horizon is genuinely
unresolved. Do not turn a late-only success into a primary win. The theorem's
.658872 shared-LTI lower bound is asymptotic, not a finite-sample threshold to
which observed noisy means can be compared as if it were an audited control.
The fixed .9/.99 oracle asymptotic risk is .499048; response_star's fixed oracle
is stronger. All finite comparisons use saved common-stream policy outputs.

If reporting a best native configuration, show that it is a selection over two
responses and retain both, rather than claiming a single prespecified method
won. There is no new calibration sweep or deployment recommendation. A future
neural study still needs fresh-task confirmation and equalized tuning/dose.

## Acquisition and audit boundary

Save the canonical draw, latent signal, rotated noise/observation, mu, native
basis/action, full moment/basis, every policy output, I17/scalar buffers and
recurrence residuals. Metadata must bind exact source hashes/config/policy order;
completion must enumerate all192 streams and hashes. Never regenerate a live
or completed stream. Plan an independent NumPy-only saved-array integrity and
arithmetic audit, not a native/RNG replay.

CPU-only with hidden CUDA and numerical threads1, exclusive large-volume root,
zero additional swap, no auto restart. Proposed hard limits:4GiB RAM,1 core,
1800seconds+5s stop grace,1600s cooperative,2GiB arrays/3GiB root. Confirm a
deterministic fixture timing margin and physical RAM/storage before admission;
if infeasible, revise/freeze prospectively before any scientific draw rather
than partially run a grid. Cumulative paid budget remains$100; spent/reserved$0.

Prospective storage correction before any scientific draw: main deterministic
two-observation fixture c7b82a confirms1,442 bytes/observation at Q1=0 and1,458
at nonzero Q1. All192 streams therefore require1,115,648,000 raw array bytes
before NPZ headers, exceeding the earlier proposed1GiB cap. Raise the local
array/root caps to2GiB/3GiB before freeze, retain every arm/seed, and verify
physical capacity. No failed scientific partial run occurred; RAM/time/swap/
CPU and paid-budget limits are unchanged.
