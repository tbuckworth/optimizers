# I15 analysis and archive review

7 September 2026. Acquisition is complete,18/18newbranches/zero failures;
this review precedes the first real analysis and inspection of numerical
outcomes. Frozen acquisition sources remain at`2bccbc6`; no trajectory,
reference endpoint, warmup or model forward is repeated by the analysis.

Main inspected the independent analyzer and collector, and a separate Codex
mathematical reviewer checked the estimator/identity semantics. The analyzer
imports neither the acquisition runner nor its optimizer core. Its complete-
state tree digest independently reproduces the I9 encoding; only new complete
states and six used I14 h100 parents are CPU-loaded. Model-only intermediates
and I14 reference checkpoint records are file-hash checked, not replayed.

The twelve primary H/M/S effects, both selectors and their earliest-tie rules,
both auxiliary metrics, all scheduled cell values, absolute h100 changes,
and fixed-label realization fitting are retained. Energy ratios are computed
within branch before equal-seed averaging, separately for101–2000and1001–2000.
Missing required contributions yield unavailable effects, never survivor means.
Actual-action defects, component/buffer energies and signed dots are separate
descriptive summaries, not clean-utility proxies or mediation fractions.

Review corrections made **before** the real audit:

- An actual action need not be an exact projector: complement-energy fractions
  are nonnegative but not artificially capped at one. Gram/action defects remain
  visible rather than being forced below a scientific-success threshold.
- Typed nonfinite failures can occur during a scheduled evaluation after an
  update, including the terminal update. Their saved topology remains admissible
  evidence; failed branches do not acquire endpoint or selector estimates.
- The archive accepts honestly recorded numerical-failure evidence when a
  pinned integrity audit passes. Integrity success is not scientific success.
- Scheduled metrics, direct component/buffer energy and zero-vector cases are
  explicit, including the ratio-of-summed-energies rather than mean-fractions rule.

The archive requires a SHA-pinned passing audit for the exact root, both phase
completion and attempt hashes, and recursively identical declared runtime
inventory. Scientific membership is exact. It losslessly compresses every
original new JSON and verifies byte-for-byte recovery; tensors and I14 archives
remain separate hash-bound sources. Output is exclusive and capped at512MiB.
No original is deleted or changed; a partial collection is retained if it fails.

Main final combined33syntheticCPUtestsPASS1.348s (`27022` terminal0/chunk`5489e0`),
including acquisition regressions,9collector tests and exact primary-magnitude/
selector/missingness checks. Analyzer settledSHA
`51c9d3b079481f4d02cf4177da0326849731aaa2b27e87b09a82aacb946930fa`;
testSHA`897b58a5b2282c215e324417b870febf42c404893a86ff6f18c5ed36725648d4`.
The real audit will use one exclusive`analysis-001` output, CPU-only numerical
threads1,6GiBhost/zeroSwap/CPUQuota100%,300sunit ceiling. It will not run until
final tests and source freeze. No cloud allocation or new training follows
from this review.
