# Complete structural recipe implementation scope

Codex / Spectral Optimizer Investigation, 7 September 2026.

Recovered clean134dd87f7810c44669c9b7187726c0037181c646, active goal and no
live native/test process. Original objective, state, next steps, recipe proposal,
repository knowledge instructions and workflow read. Previous turn was verified
progress. Resume the same I7 Step9 engineering task, not a new research run,
issue, experiment restart or paper delivery. Preserve all completed experiments
and the consumed inspection. Native attempt, supervision, frozen checkout and
both production evidence files are absent.

Implement the recorded `native-storage-recipe-next-scope.md`: explicit ordered
builders for all11 components, without calculator mutation/import swapping or
opaque metadata. Parent owns common placeholder/traversal helpers, the truthful
`storage_crosscheck_cpu` metadata role/collector integration, an inert default
cross-check entrypoint, source manifest and assembled tests. Core leaf owns9
core/history/plan/comparison builders; branch/audit leaf owns the other2. A third
leaf independently reviews design/code. Parent is sole hub; no leaf spawning,
user dialogue, external sends or commits; agree interfaces before code.

Builders return primitive dict/list/tuple trees with explicit TensorSlot leaves,
never tensors or scientific objects. Use an unregistered same-byte-length
diagnostic profile and preserve complete metadata container structure, using
actual supplied source/CPU diagnostic environment trees within32KiB/8KiB caps.
This CPU environment is not a maximum native one-device metadata observation;
the independent5J bound remains authoritative. Builders are explicit mirrors,
not calls into producers, scientific validators, IDX, training or plan makers.
All primitive, conditional/error and tensor paths must be reviewed against the
frozen original schemas/calculators/inventory. No opaque padding replacement.

Parent common API proposal: `TensorSlot`, singleton `SLOT`, `PROFILE`,
`copy_primitive(value)`, `primitive_cost(template)` (SLOT contributes0),
`slot_paths(template)`, and separate explicit materialization. Core leaf exports
`build_template(component, *, sources, environment)`, plus `parameter_rows()`,
`optimizer()`, `observer()`, `native_tensor_metadata(shape, dtype, device)` and
`envelope(payload, *, role, bundle, update, kind)` for branch/audit reuse.
Branch/audit exports the same build_template interface for its2 components.
Defaults/imports remain inert/Torch-free; no callable recipe is an execution
permission. Freeze every new recipe/helper dependency in the source manifest.

Current authorized checks are symbolic complete structural templates (no real
full-width tensor allocations/serialization), tiny CPU serializer fixtures and
metadata-role unit contracts. Use hidden/uninitialized CUDA, four thread vars1,
verified big/tmp,120s outer timeout and active2GiB RSS watchdog per focused
command; report its exact process scope. No actual11-component measurement,
native initialization, IDX, scientific plan/training, frozen checkout, native
attempt or M/A write occurs here. Production worker admission stays always
closed. After assembled code/review, separately review actual measurement
process supervision/resource envelope and fixed-slot consumption before any
real measurement. Do not infer that cooperative checks can preempt Torch calls.

Best-practice check: explicit pickle2/newZIP settings,64-byte alignment and
restricted CPU loading match the pinned Torch2.11 documentation. Restricted
loading is not a resource-exhaustion defense; an external watchdog remains
necessary. CUDA is_initialized is used for the non-initialization assertion,
not as GPU availability evidence. See [serialization semantics](https://docs.pytorch.org/docs/2.11/notes/serialization.html),
[save API](https://docs.pytorch.org/docs/2.11/generated/torch.save.html) and
[initialization state](https://docs.pytorch.org/docs/2.11/generated/torch.cuda.is_initialized.html).

Independent review refined this scope before measurement: M is prospectively
versioned to `i7_native_storage_measurement_v2` / `i7_complete_structural_recipe_v2`.
It retains the complete exact diagnostic environment tree (<=8192 canonical
bytes), not just body hashes. Source-set digest and repository revision remain
separate bindings. No production M exists to migrate. Missing environment is
allowed only for a failed source/runtime stage before metadata collection.
Diagnostic role checks include hidden/uninitialized CUDA, interop1 and CPU RNG
length5056; the scientific source/audit adapters must still reject that role.
Tiny negative guard tests exercise that rejection without scientific payloads.

The audit structural witness deliberately combines mutually exclusive adverse
branches from the union ceiling; it is not one reachable validator output.
Sources are retained only where the original schema has a sources tree (other
components retain schema-shaped digest fields). All envelope tensor-byte fields
are exact inventory totals. Full tensor allocation remains unexecuted here.

Unresolved launch requirement: reserving the fixed M slot before real work must
make hard death distinguishable from never-run, without an uncharged marker,
retry or sidecar. The existing late write_measurement helper does not do this.
An inert serializer helper is not a measurement controller or permission.
