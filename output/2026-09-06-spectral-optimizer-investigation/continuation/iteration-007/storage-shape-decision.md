# Storage-only measurement scope

Codex parent, 6 September 2026, from clean checkpoint 353ec26.
This is a dataset-free engineering continuation, not a scientific execution GO.

The current risk is whether complete evidence fits the unchanged 1-GiB cap.
Approve only an in-memory full-width storage specimen for the independently
counted required tensor fields, with explicit new storage-only marking. It
does not extend any scientific/MLP fixture validator to accept another profile.
No study root, plan generation, MNIST loading, native source step, GPU
initialization, pilot, old-experiment restart or scientific outcome follows.

The specimen must own all logically distinct tensor storages, retain rank 32,
four parameters and complete optimizer moments for all six defined branches,
and include separately stored bases, delivered gradients, flat endpoints and
float64 measurements. It also includes the exact-index fields already required
by anchor-schema section 8, now frozen as owned CPU int64 lengths 64/256/5000.
Synthetic index values are not a generated scientific plan or valid selection.

Serialize the three specimen objects separately into bounded 64-MiB memory
buffers, hash actual bytes, inspect ZIP entry sizes/compression, restricted-load
on CPU and compare tensor bytes/ownership. No disk throughput or allocated
filesystem-byte claim can follow from this in-memory measurement. Measure zero
and one tensor fills with otherwise equal metadata, to isolate payload value
effects; report both without assuming compression. The default CLI is inert.

The current PyTorch 2.11 documentation describes uncompressed ZIP64 serialization,
storage-sharing preservation and restricted loading; explicitly test independent
owned storages and use `weights_only=True, map_location='cpu'`. Python documents
that BytesIO.getbuffer provides a non-copying view and must be released before
the buffer is resized/closed. These are implementation facts, not evidence that
the complete scientific envelope will fit. No unsafe loading fallback is allowed.

Sources: [PyTorch serialization](https://docs.pytorch.org/docs/2.11/notes/serialization.html),
[Python BytesIO](https://docs.python.org/3.12/library/io.html#io.BytesIO).

Independent schema review must name still-unfrozen binding/proof/audit fields.
The measured specimen overhead and 18-fold arithmetic projection must remain
explicitly incomplete, not a full-attempt upper bound or launch certificate.
Full scientific envelopes, worst-case retained failure diagnostics, all-phase
disk accounting and a separate native-pilot decision remain required.
