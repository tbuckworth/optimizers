# Storage authority and outside-writer implementation scope

Codex / Spectral Optimizer Investigation, 7 September 2026.

Recovered clean `5549db6264d85e6387fc023fed100f13f761f0a8`, active goal and no
live experiment/test handle. Original objective, state, next steps and the
prior authority proposal read. Continue the same fixed I7 task at Step 9;
no fresh research run, issue, public push, new reminder or experimental restart.
The previous turn was verified engineering progress, not completion/blockage.

Implement the proposal in `native-storage-authority-next-scope.md`. Parent
owns the new strict authority/evidence decoder and bounded external writers,
their tests, final runtime-admission integration and source manifest. One leaf
owns the permission/journal/request pin chain and related tests; another derives
and implements the separate inert cross-check runner only after agreement on
its representation and report interface. A third leaf independently reviews
the proposals and final code. Parent is sole hub: no leaf agents, user dialogue,
commits, external sends or edits outside agreed ownership. Agree APIs before code.

The admission wrapper should preserve existing fixed fields where possible.
The measurement schema must bind all eleven ordered components, exact source
revision/digest, serializer facts and observed resources. Recompute the analytic
ceilings/ledger; measurements are mandatory diagnostic cross-checks, not upper
bounds. Supplied JSON/hash claims do not prove execution. Trust comes from the
reviewed exact-permission channel; bind the same admission pin through the
development permission, journal, all later permissions and all four requests.
Do not require a cryptographic signature or self-referential source commit.

Preserve inert, Torch-free import/default CLI paths. Supervisor pre-launch
validation must remain Torch-free; full worker arithmetic/runtime validation
may import CPU Torch after its consumed boundary. Distinguish structural/pin
checks from runtime recomputation, rather than importing Torch prematurely.

Both existing evidence slots remain <=64 KiB; later permissions remain <=8 KiB.
Use bounded in-memory encodings and no-follow descriptor-relative direct-final
exclusive writes with file/directory fsync. No temporary copy, sidecar, overwrite,
retry or unaccounted output. Partial writes stay inside their reserved fixed
slots and are preserved. Do not raise any scientific/resource/storage cap.

Only tiny synthetic CPU unit/contract fixtures are currently authorized:
hidden/uninitialized CUDA, all four numerical thread limits1, verified
`/tmp/spectral-experiment-artifacts`,120-second outer timeout and2-GiB RSS bound per focused
command. Use an active watchdog and report its process scope honestly. No
actual eleven-component cross-check, full-width specimen, native initialization,
IDX read, scientific plan/training, frozen checkout creation, attempt creation
or production evidence write occurs in this implementation scope. Those require
separate assembled resource review after final code/source freeze. Native
execution stays gated; no evidence is fabricated to make a decoder pass.

Official documentation confirms descriptor-relative low-level open/fsync,
custom duplicate-key rejection for JSON, and explicit Torch pickle/ZIP settings.
These API facts do not attest source provenance or historical execution.
Reuse existing pinned implementations and bounded readers; make the remaining
trust assumptions explicit. Sources: [Python3.12 OS](https://docs.python.org/3.12/library/os.html),
[Python3.12 JSON](https://docs.python.org/3.12/library/json.html),
[Torch2.11 save](https://docs.pytorch.org/docs/2.11/generated/torch.save.html).

## Scope refinement after recipe review (same turn, before any recipe code)

Finish the authority/writer/pin-chain portion only. The mandatory runner is
deferred to `native-storage-recipe-next-scope.md`, not dropped. Independent
review rejected temporary mutation of imported calculator globals and an
opaque JSON replacement for the source/environment container structure.
Private namespace emulation would still need a brittle implicit mirror of
mixed aliases, integer-only branches and zero-valued tensor slots. Prefer an
explicit recipe builder with mechanical structure/cost/inventory comparisons.
The leaf created no runner/test/document file and ran no specimen or test.

`native_storage_authority.validate_runtime_admission` is a deferred candidate
validator. Do not wire it to the worker yet: the existing
`final_storage_accounting.validate_runtime_admission` deliberately remains
unconditionally closed. Completing this checkpoint is neither admission nor
completion/blockage of the continuing research goal. No new metadata role or
collector changes occur here; manifest growth is only the authority helper.
