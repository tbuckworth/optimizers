# Pre-freeze evidence-conditional adapter

Codex / Spectral Optimizer Investigation, 7 September 2026.

The continuation skill supplies recovery and the recorded next-step choice.
The user's explicit autonomous continuation instruction takes precedence over
its interactive/new-issue path: stay in the same I7 run and implement the
already recorded prerequisite. No completed experiment is restarted.

Wire only the existing final_storage_accounting worker adapter to the strict
native_storage_authority runtime path. Keep exact input checks, full pin/source/
environment/ledger/runtime verification and fixed errors; no supplied status,
truthy return or missing authority may grant admission. Ordinary exceptions
reject; KeyboardInterrupt/SystemExit propagate. The externally reviewed M/A and
separately scoped native permission remain required. Absent production M/A still
means no native GO. Do not fabricate evidence to open the gate.

Use tiny synthetic parsing/dispatch/negative-control tests under hidden CUDA,
four thread variables1, verified big/tmp,120s outer wall and active2GiB sampled
wrapper/descendant RSS guard. No actual M, A, source freeze, full specimen or
native/data/plan/training operation during implementation tests. An independent
leaf checks for any other concrete source-bound execution stub before freeze.
Commit/review the final code before considering the separately scoped one-shot
CPU measurement. Future post-freeze admission supplies external evidence, never
edits code already bound into M.

The implementation validator checked Python's documented exception hierarchy
and mock-at-lookup behavior. Mapping Exception to a fixed rejection deliberately
does not catch BaseException; tests patch authority at its module lookup site
and additionally exercise its real schema/pin path with synthetic bytes.
Sources: [Python3.12 errors](https://docs.python.org/3.12/tutorial/errors.html),
[unittest.mock](https://docs.python.org/3.12/library/unittest.mock.html).
