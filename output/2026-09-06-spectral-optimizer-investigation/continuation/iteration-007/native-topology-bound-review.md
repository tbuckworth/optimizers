# Independent prospective topology review

Scope: arithmetic and primary-code topology for the unchanged I7 study. This is
not Step10 scientific audit, actual native admission, GPU/resource certification,
proof of compiled-library provenance or permission to run a study specimen.
The parent retained the hub role and final integration/commit responsibility.

The read-only independent reviewer checked:

- The W<=5J primitive lemma, including UTF8/surrogate escapes, signed LONG1/4,
  successful JSON digit-limit premise, C-pickler batch tails and alias behavior.
- Exact model/optimizer/observer/RNG/core structures; the two-calls-per-update
  repair counter proof; anchor binding copies, witness hashes versus saved RNG,
  compact historical fingerprints, plan metadata and common envelope costs.
- All three branch domains, retained15 pair/contrast rows in undefined cases,
 132/112 tensor split and all finite zero-geometry/leverage variants.
- Audit stage-prefix/fatal/partial domains,92 bitsets,4010 measurement and441
  comparison paths,5082 union failure rows,392 discordance paths and51 geometry
  paths. The4MiB terminal-text overcount is safe and is not claimed attainable
  simultaneously with every other charged string.
- The new order-independent ZIP formula, every root3 charge, tensor descriptor
  and raw storage split, and the complete82-body/3910-record aggregation.

Two **material copied-label underbounds** in the parent's preliminary core
helper were found and corrected before aggregate adoption: `encoding='torch'`
became the actual `torch_weights_only`; the independent audit schema became
`i7_anchor_numerical_audit`, not `i7_independent_audit`. A new test pins both.
The branch/audit component arithmetic had no remaining material defect.
The audit author also corrected an early proposal's scalar-record failure-site
count375→600 before implementation; final union is5082, not the preliminary4857.

Remaining review boundaries are explicit, not waived: actual JSON caps and
matched names, native layout, strides/counters, complete frozen source/proof
authentication, decoder bounds, immutable runtime serializers, per-write/global
root-plus-external accounting, filesystem allocation/free space and RSS. A
successful conditional inequality is not any of those runtime attestations.
