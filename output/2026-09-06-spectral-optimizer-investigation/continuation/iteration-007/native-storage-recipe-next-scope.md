# Next engineering scope: truthful complete structural serializer recipe

Codex / Spectral Optimizer Investigation, 7 September 2026. Proposal only.
No specimen, measurement, native permission or experiment restart is authorized
by this file. Read state and verify handles/paths before continuing.

The strict authority schema/writers and v2 permission chain are implemented
separately. The actual worker gate still always raises. The mandatory actual
eleven-component serializer diagnostic, independent review A, final source
freeze and native permission are not yet available.

Implement an explicit runner-owned structural recipe for the eleven ordered
components in native_tensor_inventory, with no producer, training, IDX,
scientific-plan, validator or test-fixture execution. Do not mutate imported
calculator globals or temporarily swap imports. Review rejected that shortcut:
core copies aliases/constants by value, branch has several bare-zero tensor
slots, and audit branches on exact integer cost types. Private namespace
emulation still needs numerous hand-maintained rewrites. Explicit builders are
longer but make the shape and source dependencies inspectable.

For each complete structural recipe mechanically compare ordered paths, all
tensor slots, dtype/shape/storage count/raw bytes to the fixed maximum inventory;
bound its primitive/pickle representation against the independently recomputed
component ceiling. Preserve negative/error variants and the all-adverse audit
maximum. Serialize sequentially, one <=64MiB buffer; restricted CPU roundtrip,
stored ZIP entries/raw storages, canonical comparison JSON and all eleven rows
must be checked. Failed reports retain exact membership with failed/not_run
rows; no successful admission may use them. Measured sizes, hashes, resource
and roundtrip records are diagnostic reviewed self-reports, never substitutes
for the prospective bound or proof of historical execution.

Match the authority wire schema or prospectively review/version a necessary
change; no silent label relaxation. Add every runner/recipe dependency to the
frozen source manifest. Keep import/default CLI inert and Torch-free. Tiny
structural unit fixtures can be scoped separately under the established hidden
CUDA/four single-thread settings/big-tmp/120s/2GiB active parent-RSS watchdog;
do not allocate real full-width specimens during their implementation tests.

After code, tests and independent review, separately review the assembled
actual diagnostic runner plus external process-group watchdog against 120s,
2GiB RSS and64MiB buffer limits. Cooperative Python checks cannot preempt a
blocked serializer; do not claim whole-tree RSS limits from RUSAGE_SELF alone.
Freeze clean C/S including the runner and decoder before any actual measurement.
Retain M only in its already charged fixed outside-checkout slot; an existing
zero/partial slot is consumed, not retryable. Review exact M independently,
then write A separately and bind A through all v2 permissions/journal/requests.
Only after this work should the currently closed worker adapter be considered
for connection to the fresh runtime validator and separate native permission.
