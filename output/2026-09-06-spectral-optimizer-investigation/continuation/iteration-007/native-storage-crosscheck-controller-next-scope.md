# Next scope: one-shot CPU measurement controller

Continue the existing I7 investigation from the recipe checkpoint, after
rereading the original objective/current state and checking goal, process
handles, worktree and exact consumed/absent paths. This proposal is not launch
permission, measured evidence, a native GO or a reason to repeat any experiment.

The explicit11-component symbolic builders and tiny serializer primitives are
implemented and tested. Default entrypoints remain inert. Next implement and
independently review the missing orchestration before any actual full-size
specimen/measurement:

Only after that assembled review can the separately approved actual measurement
be considered. Do not write a reviewed A from self-fabricated fixtures. The
production worker gate remains unconditionally closed until real evidence,
review authority, source freeze, resource controls and admission integration
are all satisfied. Native MNIST/pilot/primary/sensitivity scientific work stays
unchanged and unstarted. Preserve the two-hour reminder while the goal remains
unfinished; respect any newer pause, cancellation or redirection.
