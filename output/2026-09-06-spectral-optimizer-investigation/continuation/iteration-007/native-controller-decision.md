# Development controller integration decision

Codex / Spectral Optimizer Investigation, 7 September 2026.
Base: `6b51871796eca3c0ea47c81b728ed18b9b01d250`.

The preceding checkpoint made concrete component progress, not a scientific
finding. Continue the frozen direction/input-norm investigation. No live study
process was found; inspection001 remains consumed. This is implementation and
dataset-free CPU-test authority, not a native GO or a retry.

Assemble the development path from its existing bootstrap: exact saved plan,
live capture-on/off source pair, both registered branch groups, and the sealed
development boundary. Each work operation must retain started before producer
work, authenticate its actual output/receipt bytes, then retain sealed. A valid
unequal source pair retains its full completed evidence before terminalizing;
it must never reach either branch. Other failures preserve the actual prefix
and obey absolute ArtifactStore terminality, without a post-terminal policy write.

Prepare the four long plans only after a successful development boundary. Start
the primary guard before preparation and retain that same guard for the later
primary GO. Do not implement a boolean that turns a pilot pass into authority
for primary training. Later permissions, phase environments, the separate CPU
auditor and boundary reopen are still explicit integration work; entrypoints
remain inert. No new scientific identities, masks, tolerances or limits.

Add paired, validated launcher-entry clock origins to RuntimeGuard. Elapsed
checks include pre-constructor work; first constructor observations remain the
clock-regression baseline. No probe/limit override in scientific mode, no
cross-process reuse of CPU origins, no clock reset, and no absolute origins in
scientific resource records. The source and branch adapters use the exact
sealed plan, not just a matching plan reference. Parent owns the new controller;
leaves own the guard delta and the new branch adapter; review is independent.

Controller tests may use primitive synthetic-contract payloads with the fixed
scientific schedule. This is actual-file/order/failure evidence, not numerical
or hardware evidence. Branch tests use only the registered tiny CPU MLP and
synthetic IDX, under the existing bounded verified-big-volume test discipline.
No scientific plans, MNIST reads or CUDA initialization in these tests.

Python documents monotonic elapsed differences and current-process CPU time,
which excludes waiting. Therefore use separate entry observations in the same
process; do not subtract a previous process's CPU timestamp. RUSAGE_SELF is a
process measurement, not a sum of future audit-child consumption. These facts
support the clock design, not a hard-preemption or native-feasibility claim.

- [Python time](https://docs.python.org/3.12/library/time.html)
- [Python resource](https://docs.python.org/3.12/library/resource.html)

This checkpoint must not be presented as the complete launcher. Pre-import
enforcement in bootstrap, process exit, partial-root construction failures,
later permissions, the separate audit process, output collection, total shared
storage and the final reviewed clean source set still precede native execution.
