# Later-phase integration review

Codex / Spectral Optimizer Investigation, 7 September 2026.
Base: `4f8e58c1390daeba613e88ca3e4afd9c06df2cb5`.

## Scope and evidence classification

This checkpoint connects the existing frozen later-phase policy to explicit
boundary permissions, source/branch producers and sealed-input CPU auditing.
It is engineering evidence from synthetic contracts and registered tiny CPU
fixtures, not a native execution, scientific outcome or launch authorization.
The research workflow keeps that distinction explicit; no durable conclusion
or knowledge-base claim changes here.

The controller preserves the 49 operations, 90 successful policy records,
three primary bundles, separate sensitivity bundle and 16 independent audits.
All three primary sources precede primary branches. Preparation of the four
plans uses the same primary entry-origin guard as the ensuing GO and work.
An independently consumed permission is required at each closed boundary.
No default/import entrypoint starts work, no phase automatically starts another,
and an adverse audit retains its actual artifact and terminal failure without a
fabricated post-terminal sealed policy record.

GPU-source provenance and CPU-auditor provenance are separate exact bindings.
The source manifest now includes all ten previously omitted execution-bearing
adapters/controllers, for 55 entries. Permission and environment root copies use
the same store budget. External journal, failure and three permission/transition
pairs have a combined 64 KiB bound within the existing shared failure reserve;
this does not enlarge the 1 GiB root budget or establish native fit.

## Independent review and corrections

The source-owner audit adapter and parent controller/helper/manifest changes
received independent static PASS verdicts. The final control-owner snapshots
also received independent static PASS, with no remaining material finding. The
assembled suite passed as handle10457/PID1312423: 457 tests in 482.652s.

Material issues found during implementation were corrected before final freeze:

- A consumed permission must match the actual predecessor payload and writer
  receipt, not merely a well-shaped supplied reference.
- Each phase record must match its retained phase environment; sensitivity must
  retain the primary environment bytes, while CPU audit retains a distinct role.
- Missing all transition files must not bypass retrospective permission checks.
- Reopening a writer must reauthenticate held prior membership, payload/receipt
  references and metadata after inspection; an earlier unlocked snapshot is not
  sufficient evidence that the inputs remained unchanged. Held rescans reject
  newly appeared failures, unknown files and future permissions, and a guard
  checkpoint precedes the first metadata write after the rescan.
- Partial marker writes and unavailable failure records must be described
  accurately, without claiming unretained evidence exists. Invalid transition
  data must yield a structured nonresumable inspection result.
- The controller revalidates the exact acquired-transition structure and actual
  marker before adopting it. Failed audits cannot append generic success records.
- The generalized artifact-reference helper must not apply the former boundary
  record's 128 KiB read limit to larger payloads. It now derives payload pins from
  the store inspector's authenticated receipts; a 140 KiB nonsemantic fixture
  covers this without creating scientific arrays.

Parent briefly inserted then removed an unnecessary source-result invariant
override after confirming the adapter already returns that field. No execution
used that intermediate edit.

## Verification

Parent's initial 11-test focused diagnostic completed in 119.028s
(handle51527/PID1301332), with two test-helper errors: an incorrect exception
class name and a missing sibling-module path in the isolated import check.
After correction, **11/11 passed in 130.893s**
(handle33010/PID1303995). This includes the complete actual-controller synthetic
90-record close/transition sequence, not numerical scientific payloads.

The source leaf reports **7/7 in 26.696s**, including the actual registered tiny
source-to-branch-to-audit pipeline and retained fatal audits. An intermediate
helper forwarded the new auditor-environment keyword to the source adapter,
causing five setup TypeErrors; it was fixed before the passing run.

Control leaf final verification: transitions **9/9 in 45.624s** (handle59732),
native control **16/16 in 47.578s** (handle59746), both terminal. Intermediate
transition diagnostics exposed fixture defects: two errors from the wrong CUDA
probe return shape; one error from a retry helper hitting create-only permission
creation; one failed assertion because the replacement fixture used the identity
JSON encoder rather than the store's receipt encoder. All were corrected before
final focused verification. These diagnostics were not native attempts.

Optimizer regression: **17/17 in 1.295s** (handle70389 terminal).
Reminder regression: **6/6 in 4.512s** (handle66434 terminal).
Knowledge lint: 14 pages, 11 indexed content pages, pass.
All checks used hidden CUDA, single numerical threads and big-volume TMPDIR.
Focused runs overlap the later assembled total and must not be added twice.

Final assembled I7 verification: **457/457 in 482.652s**, handle10457/PID1312423
terminal, exit zero. Including optimizer and reminder regressions gives **480
distinct tests, 27 new**. All frozen implementation/test hashes remained unchanged
during this assembled run. Exact hashes and commands are in the adjacent
`scientific-phase-integration-checks.json`. No completed experiment was restarted.

Component commit: `08e6443410741139aaa816b49d91637f2668fe6f`, normal commit hook,
no secret-scan bypass. Post-commit collection authenticated all 55 source members
on the clean worktree, CUDA uninitialized (handle84664 terminal). An initial
read-only invocation referenced the profile constant on the wrong module and
failed before collection (handle80157); the corrected invocation passed. The
checks file retains the verified revision/digest. These checkpoint notes are
not new source permissions; actual launch requires its own fresh binding.

## Remaining gates

This is not an assembled launcher. Separate CPU-process supervision and observed
exit status, final shared storage/source accounting and resource ledger, final
assembled review, prospective development permission and each later scoped
decision remain required. A cooperative in-process guard is not hard process
preemption. No real plan, MNIST read, native attempt or scientific experiment has
been started at this checkpoint. The consumed inspection001 is never retried.
The original two-hour reminder remains enabled and the research goal is active.
The study source worktree must remain clean at one pinned revision throughout
all phases; state/progress writes and permission reviews must not change that
source revision between transitions.
