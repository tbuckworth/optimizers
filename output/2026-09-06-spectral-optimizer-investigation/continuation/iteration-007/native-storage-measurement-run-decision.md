# Decision: one actual bounded CPU measurement

Codex / Spectral Optimizer Investigation, 7 September 2026.

Under the user's original instruction to continue local hypothesis/testing work
and the current explicit continuation reminder, main approves the single CPU
structural measurement already scoped in native-storage-measurement-run-next-scope.md.
This is within the existing desktop CPU envelope, not permission for native
training, A, a phase permission, paid compute or a repeated experiment.

The complete controller/worker/slot/service passed109 selected checks and final
independent static review. The source-bound evidence-conditional adapter now
passes50 selected checks (11 new) and independent execution-path review; no
remaining unconditional source-bound stub was found. A second independent
review found the actual11-component CPU envelope coherent and bounded with no
additional implementation blocker. Unknown full-size runtime/memory fit remains
an experimental outcome, not a reason to increase limits or retry.

Before launch, commit this decision and all reviewed source changes. Verify the
clean committed80-member source set in the original implementation checkout,
derive a prospective S for the exact future frozen root from the same committed
source bytes, then create only the exact absent frozen checkout at that C.
This expected S is not claimed to be observed future measurement history.
The post-reservation worker must freshly authenticate that exact C/S/root.
No numerical/source-collection rehearsal is run in the frozen root before M.

Immediately recheck fixed UNIT/M/A absence, no live measurement/native handles,
the big-volume UUID/free floor, and exact committed code. Invoke only the
reviewed controller's explicit launch_one_shot API once, with those exact C/S.
Retain/follow the unit invocation and handles, wait for terminal outcome, inspect
existing M, and never restart if output is lost or M is zero/partial/failed.
Do not create an alternate service, evidence slot or inspection.

The fixed verified user service has120s RuntimeMax,2GiB MemoryMax, swap/zswap0,
128 tasks, OOM group kill, whole-group shutdown,1s stop grace, no restart/core/
journal payload. Worker work stops by110s; finalization begins before115s.
Four numerical threads1, hidden/uninitialized CUDA, exact secret-free environment,
verified big/tmp, one component at a time, bounded64MiB serializer buffer,
56KiB candidate and8KiB observed supervision under64KiB M. Sampled RSS is distinct
from kernel charge. M's observation ends before its own final fsync; later review
must also check terminal service result/exit and cleanup.

No data/IDX/scientific plan/native initialization/training/paid call is included.
M is the only measurement-data output. Transient unit/cgroup metadata is OS
supervision state. After completion, inspect/review M and the terminal invocation;
do not issue A or native GO without the separate trusted review/permission step.
The frozen code is thereafter immutable for this attempt.
