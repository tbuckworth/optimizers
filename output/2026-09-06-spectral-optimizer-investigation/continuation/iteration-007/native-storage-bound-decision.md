# Native storage-bound implementation decision

Codex / Spectral Optimizer Investigation, 7 September 2026.
Recovered base: `989b9876ea1cd74a00d470d4040a527d74c1c256`, clean worktree,
active goal, no live experiment/test process. The preceding turn was verified
engineering progress, not a scientific result or a blocked turn. The existing
two-hour reminder is unchanged.

## Bounded work

Continue the proposal in `native-storage-bound-next-scope.md`. Establish a
reviewable analytic upper bound on every encoded payload under explicit admitted
runtime layouts. The earlier conditional subtotal is not a fit. A successful
specimen alone is not a maximum. Preserve the scientific membership, estimands,
seeds, phase permissions, 1-GiB logical cap and all existing time/memory limits.

Three leaf tasks support the parent: a protocol-2 pickle-bound derivation, a
pinned Torch ZIP-bound derivation, and independent read-only review. The parent
owns exact payload topology/counts, admitted-layout/evidence schema, integration
and state. Use the research skill's preferred available Codex worker model for
these bounded tasks; no worker spawns agents, talks to the user, commits, launches
native code, reads IDX, generates study plans or runs a storage specimen.

First agree proof assumptions and interfaces; only then implement the bounded
encoders/admission checks. Proposed primitive/ZIP helper modules and tests may
be added by their owners after parent agreement. Shared-file edits require
coordination. Every execution-bearing dependency joins the final source manifest.

The next candidate measurement remains the independently reviewed small scope:
11 component kinds, baseline/max-admitted representatives, sequential bounded
memory, hidden CUDA, one numerical thread, no data/plans/training, 120 seconds,
2 GiB RSS, 64 MiB per serialization buffer, and at most one 64-KiB canonical
result. This decision does not run it: a later explicit record must identify the
frozen implementation, actual resource scope and reviewed proof before execution.
If proof inequalities fail, report that; do not present measurements as bounds.

Native admission remains fail-closed until actual evidence and its decoder are
reviewed. No native GO is issued, no frozen source checkout is created, and the
consumed inspection001 is never retried. An engineering pass does not complete
the autonomous research goal.
