# Complete prospective payload topology: bounded scope

Codex / Spectral Optimizer Investigation, 7 September2026.
Recovered clean base `f55c4aee937d96f9d07e8da3ce2d29b13d5a27a5`; goal active,
previous turn verified progress, no live experiment/test handle. The original
user brief and authoritative state were read before continuing.

Continue the missing whole-study primitive/container proof identified in
`native-storage-bound-progress.md`. Preserve all82 payloads, scientific seeds,
estimands, adverse evidence, resource limits and consumed native inspection.
This is not a new experiment, a study specimen or native GO.

The parent owns a small Torch-free arithmetic layer and core/anchor/witness,
source-completion and plan bounds. Two leaf workers derive branch/measurement
and audit bounds from the actual constructors/validators. A third read-only
leaf independently reviews the inequalities and later code. Workers do not
spawn agents, talk to the user, commit, modify shared files, run data/plans or
execute a native or full-width storage specimen. First agree the arithmetic
interface and source-derived shape/domain assumptions; then implement helpers.

Candidate arithmetic uses worst-case five-byte memo operations and no alias
savings: literal/string-byte ceilings, bounded integers, floats, bool/null,
list/dict/tuple framing, finite alternatives and repeated fixed rows. Tensor
pickle/storage terms come from the already reviewed exact descriptors and
serializer formulas, not newly created tensors. The complete ZIP ceiling must
either bind actual first-encounter order or use an order-independent maximum
padding charge. A sample cannot supply the primitive topology theorem.

One candidate simplification needs independent proof before adoption: for an
exact-type, acyclic, finite JSON primitive subtree with compact ASCII length J,
its no-alias protocol2 subtree cost is at most5J (root PROTO/STOP are separate).
If established, this bounds admitted source/environment subtrees without the
earlier unproved64x allowance. JSON key coercion, integer widths, float encoding,
escaping, batch tails and repeated identities must be covered explicitly.

Permitted checks are small CPU-only unit/arithmetic tests: CUDA hidden and
uninitialized, all four numerical-thread limits1, verified big/tmp temporary
parent,120-second timeout and2-GiB RSS ceiling. They must not call existing tiny
experiment producers or rerun completed full-width component measurements.
No complete11-component specimen is authorized by this decision. Any later
measurement must have its own frozen/reviewed scope as previously specified.

Keep `validate_runtime_admission` fail-closed until the entire bound, canonical
evidence, decoder, actual writer enforcement and source membership are reviewed.
Even a completed storage proof is not execution authority or completion of the
research goal. Update state and commit verified work before ending the turn.
