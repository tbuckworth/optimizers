# Later-phase integration decision

Codex / Spectral Optimizer Investigation, 7 September 2026.
Base: `4f8e58c1390daeba613e88ca3e4afd9c06df2cb5`.

Recovery found the active goal, clean worktree and no remaining recorded native
or test process. The preceding turn was concrete, verified engineering progress.
Resume the original current-policy-state conditional response study, not a new
experiment or research question. No native GO is issued here.

## Authorized implementation

Complete later-phase callable integration over the existing fixed policy:
primary preparation and three sources before any primary branches; one separately
permitted sensitivity source before its branches; independently permitted CPU
audit of the exact 12 primary and four sensitivity groups. Preserve all design,
identity, mask, numerical, resource and no-retry rules. Scientific results remain
unobserved and the default/import entrypoints remain inert.

Control leaf owns `phase_transition.py` and tests, plus the necessary
`native_control.py` inspector metadata integration/tests. It first proposes the
exact permission/return API for parent agreement. Permission must bind actual
root/header/journal/prior boundary, source commit/hash and expected fresh phase
environment. It is consumed create-only before any phase work. A partial
transition must never make an earlier boundary writable again. Primary's entry
origin/PID guard covers preparation through GO without reset. New metadata uses
the existing root and budget, not another study root.

Source leaf owns `native_audit.py` and tests: an explicit per-anchor adapter from
actual sealed plan/anchor/witness/branch bytes to the existing independent CPU
auditor. Hidden CUDA and no initialized CUDA are mandatory. The original native
environment still binds the source artifact; fresh CPU environment binds the
audit. Preserve actual failed audit artifacts and terminal state, and caller RNG.
The adapter alone does not prove a separate process or grant audit permission.

Independent reviewer checks both design and frozen code. No worker may spawn
agents, interact with the user, commit or run native/scientific work. Verification
uses existing registered tiny CPU fixtures and synthetic metadata, hidden CUDA,
single numerical threads and TMPDIR `/tmp/spectral-experiment-artifacts`. No full-shape seeded
study arrays, actual MNIST reads, new attempt roots or repeated historical runs.

## Platform check and remaining assembled work

Python documents nonblocking flock acquisition and explicit subprocess arguments,
environment and exit codes. Use separate descriptor acquisition for a closed
writer transition; do not equate an unlocked snapshot with permission. A future
CPU-audit launch must use a fresh interpreter with hidden CUDA and an observed
process result, not inherited initialized native state. Process creation and
blocking calls are not universally preemptible; the existing cooperative limits
and explicit unobserved-exit caveat remain in force.

- [Python fcntl](https://docs.python.org/3.12/library/fcntl.html)
- [Python subprocess](https://docs.python.org/3.12/library/subprocess.html)

Final source membership, complete shared storage accounting, outer process
supervision/exit observation, prospective development review/GO and each later
phase decision remain necessary before native execution. Passing these CPU
checks does not change any scientific conclusion or silently authorize launch.
