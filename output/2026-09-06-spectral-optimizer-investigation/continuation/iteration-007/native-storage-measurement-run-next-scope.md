# Next scope: reviewed actual eleven-component CPU measurement

Continue the same I7 research task, after checking goal, original objective,
state, live handles, worktree and the exact fixed paths. This document is a
prospective scope, not evidence that measurement ran or a native GO.

The slot/service/controller/worker implementation,109 selected tests and final
independent static review are complete. Perform clean source verification, then
resolve this concrete dependency before any frozen checkout or actual M:

Then review the concrete actual-run envelope below. No additional broad
implementation phase is warranted unless a specific defect is found.

## Fixed envelope for the next actual action

Exactly one sequential CPU structural cross-check of the11 existing complete
templates, using the pinned Python/NumPy/Torch/pickle/ZIP configuration and the
unchanged diagnostic profile. No data download, IDX, scientific plan, native
initialization, training, cloud compute, CUDA availability query or consumed
inspection retry. Largest raw component is the33MB-class branch result; only
one component and its bounded64MiB serializer/CPU roundtrip are live at once.
Actual all-component runtime/memory fit is still unknown; passing tiny tests
does not waive the120s/2GiB service limits.

Use only the fixed frozen checkout
`/private-artifacts/repositories/optimizers-i7-native-frozen-001`.
Creating this checkout is a separately reviewed normal local preparation step,
not a reason to repeat a completed or live run. It must stay at the exact clean
reviewed revision C. M/A stay outside it at their fixed original-repository
paths; no root-equivalence waiver. If any path already exists, inspect its
authoritative state and do not overwrite or retry.

Expected S must bind C, all80 committed source entries and that exact future
root. An expected source record derived from verified committed blobs is a
prospective expectation, not an observed measurement. The worker must freshly
authenticate actual clean sources and that exact S only after M is reserved.
Do not run a numerical/source-collection rehearsal in the frozen measurement
root before consuming M, and do not pass a digest from the original root.

Confirm the fixed production service is not loaded and both M/A are absent,
the big-volume UUID/free floor still match, and no measurement or native
process is live. The only launch path is the reviewed controller's explicit
`launch_one_shot(expected_commit=C, expected_source_set_sha256=S)` API.
Do not invent an alternate unit name/path, direct subprocess fallback or retry.
The controller validates actual service controls and then reserves M before
spawning numerical/source/recipe work.

The reviewed service is120s RuntimeMax,2GiB kernel charged-memory cap,
swap/zswap0,128 tasks, OOM group kill, whole-cgroup shutdown,1s stop grace,
no restart/core dump or worker-output journal. Internal work deadline110s leaves
time for validation; finalization may begin only before115s. Sampling reports
process RSS separately and does not claim a kernel aggregate-RSS cap.
The exact allowlist hides CUDA, fixes four thread variables1, neutralizes Git
global/system config, fixes PATH and uses verified big/tmp. No secrets needed.

## Completion/failure and review

Preserve the exact unit invocation, observed process handles and M identity.
Any cutoff first requires checking those handles and existing M; never launch
again merely because tool output was lost. An empty/partial M consumes the
shot. A complete candidate requires independently observed exit0 and no remaining
workers; a structured failed candidate requires observed exit1. Hard failure or
untrustworthy candidate output must not be replaced by fabricated JSON.

After the unit is terminal, independently check its invocation/result/exit,
empty cgroup, exact committed sources and M's strict schema/byte pin. Recompute
the unchanged complete analytic ledger; measured component sizes corroborate
the structural theorem but do not replace it. Only a genuinely complete,
reviewed measurement can support the separately trusted A writer. Neither this
scope nor M itself authorizes A, a native phase permission or expanding the
experiment. All source-bound code integration must already be frozen; subsequent
steps supply independently reviewed external evidence/permissions, not code edits.

Report negative/partial/unknown outcomes plainly, preserve no-retry state, and
continue safe in-scope research rather than silently resetting an attempt.
Keep the reminder active while the user-requested research goal is unfinished.
