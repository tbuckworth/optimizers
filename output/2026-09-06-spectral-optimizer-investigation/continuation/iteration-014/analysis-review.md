# I14 independent analysis review

7 September2026, before confirmation outcomes were read or a real artifact
audit was launched. Independent leaf review PASS; main reviewed the source
interfaces, aggregation, selection, failure and provenance paths. Acquisition
remains unchanged at`d48f20a`; analysis/collection are separate CPU-only work.

Final analyzer SHA256
`b8654e32113bd5d177b44a523a80328ff1f4f36ef8fc08f0dbb11bb244726d8c`;
test SHA256
`b2dab287de5fa119616073d930f7a5cb26e806ff75e52aa82daff6dda1464118`.
Main final combined34/34synthetic CPU tests PASS3.125s, handle5201 terminal0/
chunkdee45c, CUDA hidden. This includes8independent scalar-analysis tests,
2lossless-collection tests and24acquisition tests. Repeated synthetic tests
are not additional scientific trials or GPU experiments.

Review corrections made before real analysis: independently recompute startup
failure fingerprints; bind every consumed file to completion and reject extra
or partial entries; bind dataset/label-derived corruption counts; record
analysis source hashes separately from acquisition; distinguish raw CE from
positive-good CE reduction; preserve unavailable estimands without survivor
averaging; match partial calibration candidate schemas. Syntax and fixture
errors during preparation were corrected before the final passing suite.

Twelve endpoint effects, eight descriptive base interactions, both auxiliary
metrics under both validation selectors, all checkpoints, absolute progress,
R_zeta/confidence and per-step scalar geometry are represented. Full-trajectory
leakage ratios are retained; a separately named steps101+ window tests the
prospective SGD-span prediction without pooling in unfiltered warmup. These
supporting diagnostics add no primary outcome or acquisition intervention.

Audit boundaries: CPU file hashing and complete-state tree digests, saved
scalar evaluation/diagnostic identities and deterministic plan reconstruction.
No model forward, optimizer step, GPU, or per-step vector replay. Intermediate
model files are hash-checked rather than semantically replayed. Three seed
bundles are replications conditional on reused MNIST, not independent datasets.

Execution after confirmation completion: one exclusive`analysis-001` output,
CPU-only transient unit, one numerical thread/CPUQuota100%,6GiB RAM/zero swap,
300s unit ceiling/Restart=no. Collector separately creates one exclusive
`raw-results` archive, preserves every JSON byte including phase attempts,
caps original JSON512MiB and compressed output128MiB, and keeps bulk tensors
hash-bound on the large volume without claiming a backup. No training restart,
paid compute, source mutation or user-approval gate is involved.

## Post-run root-layout exception (original audit retained)

The first real audit finished with exactly one error: an extra EMPTY
`torchinductor_titus` directory created under the run's TMPDIR. All other
1,199,604 checks passed, including502declared file hashes and162complete-state
digests. Main used the assumption-debugger skill to distinguish an overly
strict layout assumption from missing/material evidence. Exact read-only root
and directory inspection supports the former; no source output was removed.

The original audit remains status`fail`, with pinned audit/summary hashes.
Independent review accepts only a separately recorded exception disposition:
exact root + empty named non-symlink directory, no-follow descriptor identity
and emptiness checked before/after; original audit/summary/source bindings;
all declared artifact hashes and completion/attempt records rechecked. No
forward, optimizer, tree-digest, scalar-aggregation or full-audit rerun.
Terminal/attempt hashes first recorded during diagnosis are explicitly labeled
as such, not retroactively attributed to the original audit.

Collector001 failed before output creation. The corrected collector must
receive the SHA-pinned supplement before creating a new`raw-results-002`
archive, validates it against original audit/summary and directory/source
identity, and rechecks after collection. Nonempty, hidden/nested, symlink,
same-name file and other unexpected entries are not exempted. Original failed
checks are preserved; no experimental outcome or protocol was changed.
