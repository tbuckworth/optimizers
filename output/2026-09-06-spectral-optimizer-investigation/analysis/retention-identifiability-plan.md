# Retention identifiability follow-up — bounded analysis scope

7 September 2026; recovered clean commit 57f4303. The previous goal turn made
progress by completing the mathematical synthesis. Goal active; failed I7
service invocation unchanged with MainPID=0; immutable checkout clean and
two-hour reminder enabled/active. No process is being resumed or restarted.

## What is new, and what is already known

The existing label-noise-gradient-decomposition note already derives the
expected soft target and nonzero corruption residual. Do not count that as new.
Iteration 005 already records strong clean/residual opposition and warns about
cross terms. Extend that evidence with a sharp geometric bound on any ideal
projector's normalized-retention contrast, and derive the fresh-label covariance
spectrum and its qualified relation to curvature.

## Bounded retained-data calculation

This is post-hoc descriptive analysis, not a prospective experiment. The
previously reported cosine and retention results are already known. Inspect all
12 original iteration-005 snapshots (seeds 3,4,5; steps 200,500,1000,2000) and both
stored widths. Read only summary.json and its 12 original snapshot JSON inputs;
verify each input hash against the committed summary and equality to its copied
raw record. Do not load tensors, checkpoints, datasets or model code; do not
run a historical producer, summarizer or audit.

Compute raw cosine from the saved Gram entries, the ideal geometric ceiling
sqrt(1-cosine^2), the signed native clean-minus-residual retention gap, and the
absolute gap/ideal-ceiling diagnostic. Preserve every state and both widths.
Average the four states within each seed before averaging seeds; report the
final-state panel separately. These ratios are descriptive native-action
comparisons against an ideal benchmark, not floating-point operator certificates
or independent replicate counts. Do not claim cosine around -.9 forces a tiny
gap: the resulting ceiling may leave substantial headroom.

## Scientific decision

Determine whether a weak retention gap is intrinsically uninformative here, or
whether geometry only limits it partly. Identify the missing soft-target versus
realization-specific measurements, without claiming that existing aggregate
Gram entries can reconstruct an unmeasured gradient. Update knowledge only for
a durable distinction supported by this derivation and retained evidence.
