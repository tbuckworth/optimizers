# Main decision: consolidate the result and design a faithful strong-regime comparison

Codex — Spectral Optimizer Investigation · 10 September 2026

## Reflection

The fixed clean multiview experiment is complete and audited. Four-view
observation improves secondary shifted-image learning, but the primary
accuracy/CE comparison is mixed and both raw controls are substantially better.
Do not promote this candidate as a practical rescue or silently choose its
secondary endpoint. No noise-protection transfer test is selected for observer4.
The result adds a useful boundary, not proof that every spectral recipe fails.

Main read the complete [historical-regime comparison](regime-comparison.md)
and checked the original stable report and model/normalization source. The
stronger stable global-rank200 regime has an actual positive, not just a
legacy-numerics story. Small rank32 tests materially differ in model, training
population, normalization, absolute exposure and projection dimension. The
earlier noise law implies about81% actually wrong, so label-law differences
alone are not a persuasive escape from the new80%-wrong result. Historical
test-based selection and early peaks remain important practical qualifications.

## Ranked next steps

1. **Consolidate the paper and unified account.** Integrate the clean,
   wrong-label, local-direction and multiview sequence with a plain practical
   conclusion and explicit regime boundary. This is source-linked writing,
   not new science. It must preserve useful protection/grokking evidence,
   ordinary augmentation's strong result, and the limited secondary improvement.
   Needs: existing audited scalars and manuscript files; no paid resources.
   Reject any framing that substitutes small local positives for training
   usefulness or claims that the strongest original configuration was tested.

2. **Design one fixed strong-regime augmentation bridge.** Existing leaf
   jlens_completion_followup now owns a design-only note for stable global
   rank200 versus raw AdamW, each without/with one-view translation, fresh
   paired seeds and a predeclared noisy late horizon. Keep the historically
   successful larger architecture/normalization/optimizer recognizable; resolve
   validation versus untouched test roles prospectively and disclose departures
   from historical reproduction. Record useful progress, fixed endpoints and
   validation-selected stopping from the same trajectories. Needs: source/
   scalar review and a conservative local3090time/memory/storage budget;
   exact protocol/source/resource admission before any execution. No new
   acquisition, seed roster, archive or paid reservation is created by this
   decision. If the faithful comparison also favors augmented raw, the practical
   objection is stronger; if filtered augmentation adds value, report the regime
   boundary rather than erasing this small-model result.

The second step is motivated by a pre-existing positive configuration, not
searching the new experiment's failures for winning hyperparameters. It is a
specific comparison of the strongest recorded version against an important
standard technique, aligned with the user's requested steelman and practical
question. Whole-goal completion is not implied by finishing this panel.

## Not selected