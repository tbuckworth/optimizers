# Main decision: prepare the small content × template diagnostic

Codex — Spectral Optimizer Investigation · 11 September 2026

Main read the complete [candidate design](proposed-design.md), SHA256
`c841cfd32b95892991a10e9e7b6d9cec78e93f55debce533f046d3bb7dece143`,
commit `e0ef8497cd5c8a630d3943960822e578e86fbb79`, including all 16 exact strings.
Accept it for bounded implementation under the existing autonomous J-Lens
authority; no user approval gate is introduced. This note is not a model launch.

The next implementation should freeze those strings into exact JSON in the
specified order, implement a fresh guarded forward-only entry point and
fabricated tests, then receive source review before one tokenizer preflight.
After all inputs pass unchanged and resource/live-handle checks pass, admit
one new 16-prefix frozen-model measurement with the existing U32 and source
mean. No old CLI stage may be restarted, no old result altered, and no template
or prefix changed after scores. In particular, the prior Wikipedia adapter's
24-row/16-word guards cannot be bypassed or silently relaxed in place.

The prospectively fixed PC4 prediction is O−P > 0 in all eight pair/template
cells. Show all magnitudes, zero/negative cases and PC1–PC3 descriptive scores.
The two templates retain the same proposition and entities; their active/passive
syntax changes word order, length and distance from the action verb. The shared
ending controls terminal-string identity, not semantic state or all formatting.
No new reader, PCA, lens decode, corpus collection, training or paid service.

No implementation, tokenizer or measurement had run at this decision. Existing
source/measurement/report handles remain consumed. Local compute is the intended
resource, within the unchanged $100 overall cap; paid spent/reserved is $0.
