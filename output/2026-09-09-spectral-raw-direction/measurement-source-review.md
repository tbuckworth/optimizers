# Independent source review: raw-direction new-state measurement

9 September 2026. **PASS for final source freeze and a later guarded measurement
after the acquisition is successfully terminal. No scientific or launch-blocking
source defect was found.** This is source review only. It did not inspect the
live acquisition output, load a real tensor or NPZ, run inference/training,
repeat saved-data calculations, or launch a service.

Reviewed current SHA-256:

- `experiments/measure_grokking_raw_direction_states.py`:
  `a6d01790fc54ddb1f913819a0d53fbbc5961e2913ea5ac54b820219d73ae9dd8`;
- `tests/test_grokking_raw_direction_measurement.py`:
  `dce7d371b55e2d1eeca3b41cf1aa4f01e489abdb93d3ce00276026c3737c99e2`;
- `tests/test_grokking_raw_direction_measurement_guard.py`:
  `a3480099636b5a687e66c7d3d140fc617101ed9f502df83895ecea4bcd8f8cf8`;
- `output/2026-09-09-spectral-raw-direction/guarded_measurement.py`:
  `a432f3786106898698870727d915265f74b4ce62de377864a00c2710ec9d15ed`;
- measurement protocol:
  `7149d0d6e115b95341bc9d149db121dcbe168f24449177d929659e94f7e8c5aa`;
- implementation note:
  `742abbba531d9fd4bb26a3ff3cff323f4baa60fd29742108084c917913534d96`.

## Exact input boundary

The only admitted roster is the ordered Cartesian product of fixed seeds
100--104, policy `raw_norm_matched`, and steps 1501/2000/2500: 15 unique new
states. Batch admission requires the fixed five-seed completion, exact batch
manifest binding, successful status, source/resource metadata, seed order and
absence of batch failure. For every seed it invokes the frozen acquisition
completion verifier, re-reads and compares the seed completion, verifies the
manifest/common metadata, seed-100 admission receipt, original legacy parent,
metrics hash, accepted action-fork identity, all nine artifacts and all three
history prefixes. It then constructs states only from the three registered
checkpoint receipts and rejects any missing, extra or reordered identity.

The resulting admission set contains 52 unique input receipts: batch manifest
and completion plus ten files for each of five seeds. Every receipt is bound by
path, regular singly-linked file status, size where supplied and SHA-256, and
all are rechecked before completion. First-step tensors and old model/checkpoint
artifacts are hashed for provenance but not loaded. Only the 15 new checkpoint
paths reach the hash-bound, singly-linked, `weights_only=True` loader; the
separately permitted old NPZ access is limited to the accepted structural
split/ID/null members described below.

No old collector `main`, action runner, acquisition runner or global monkeypatch
is called. `measure_one` rejects native, orthogonal and projected norm-matched
policies before checkpoint loading or extraction. The guard hardcodes the one
completed raw batch and one absent measurement child, accepts no arguments,
and requires the training unit to have PID zero, inactive state and successful
terminal result. It therefore cannot overlap the live acquisition or silently
substitute another batch.

## Outer and inherited state truth

The outer envelope must have the exact new schema/key set and must bind the
raw policy, seed, step, acquisition source map, original parent receipt and the
seed's first-step tensor receipt. Separately, the inner state must have the exact
legacy checkpoint key set/schema, fixed step, original legacy configuration,
config hash, source identity/hash, regenerated split identity, parameter layout,
CUDA identity/topology, finite complete state, exact legacy filter mutable key
set and step counter, and exact original filter configuration with
`stable_update=False`. The inner arm remains `legacy`; it is never relabeled as
the outer raw policy. Evaluation prefixes are exactly empty at 1501 and
1550..endpoint by 50 at 2000/2500.

This correctly preserves two distinct facts: the state contract was inherited
from the original legacy fork, while the outer trajectory was generated by the
new raw-direction policy. Endpoint behavior is recomputed from the new model
and compared to its own saved 2000/2500 training-grid values at absolute CE
`1e-4` and accuracy `1e-6`; step 1501 deliberately has no replay-created grid
value.

## Unchanged measurement recipe and one extraction

The adapter imports the old `Output`, extraction, Fourier-probe, structural
recipe, environment, resource, receipt and analysis functions by identity. The
accepted completed 35-state action measurement binds the current 14-file old
measurement source map, exact recipe and fixed panel; that binding passed using
receipt JSON only. The merged current measurement map contains 30 source files.
All reused helper files have no worktree diff.

For each seed, the collector regenerates the original p=113 train/test IDs,
stratified probe split and 20 row-null permutations and requires exact hashes
against the accepted native-step-1500 structural arrays. The frozen recipe is
unchanged: all 12,769 pairs, batch size 1024, both `final_hidden` and
`pre_attention`, 56 Fourier frequencies, fit-selected top five, ridge 0.001,
20 fixed shuffled-row nulls and fixed panel `[9,33,32,49,11]`. The accepted
source collector is designed to read only those old structural members; it does
not recompute old logits, activations, probes or symmetry.

There is exactly one `extract_activations` call in `measure_one`, and exactly one
`measure_one` call for each admitted state. One new model copy is loaded from
that state, with strict parameter layout/finiteness checks. Extraction is
forward-only, and CPU/all-CUDA RNG states must remain unchanged. Behavior,
probes and the frozen `analyze_state` all reuse that single saved activation
set; there is no second model extraction. Each state produces separately hashed
raw NPZ, full scalar JSON and analyzed-state JSON receipts. Completion requires
the exact 15 identities and binds all 45 result receipts plus the manifest,
input batch completion, prior recipe and source maps. It explicitly makes no
paired aggregate causal conclusion.

## Source, resource and failure controls

The source map includes collector, both new fixture files, measurement guard,
protocol, all acquisition sources, and all accepted old measurement/analysis
helpers. Sources are re-hashed at completion. The guard compares every current
hash to the same file at committed `HEAD` before replacing itself with the
collector.

The collector pins all four CPU math environments before NumPy/Torch-backed
analysis imports, sets PyTorch intra/inter-op threads to one, requires CUDA and
the exact accepted Python/PyTorch/NumPy/GPU/backend environment, enforces a
19-minute cooperative deadline, 16-GiB peak-RSS check, 3-GiB output ceiling and
1-GiB free reserve. Its exclusive atomic output writer refuses an existing
target. The service guard adds the hard controls: exact 16-GiB cgroup memory,
zero swap, exactly 100% CPU quota, 20-minute runtime, `Type=exec`,
`KillMode=control-group`, `Restart=no`, intended unit/cgroup, exact system
Python and single-thread environment. Failure output records already accepted
state receipts and re-raises; an existing partial output prevents retry. If a
failure JSON cannot be written, the complete record is printed to the service
log.

## Synthetic verification

Command:

```text
OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 \
python3 -m unittest discover -s tests -p 'test_grokking_raw_direction_measurement*.py' -v
```

Result: **10/10 fixtures passed in 0.101 seconds**: eight collector fixtures and
two guard fixtures. They used only synthetic JSON receipts, tiny in-memory
schema tensors, mocks and temporary files. The tests cover exact roster/helper
identity, 52-file admission, changed/missing/failure rejection, outer/inner
separation, filter/device/finiteness/evaluation guards, fixed grid tolerances,
old source-receipt binding, pre-load rejection of old policies, service limits
and successful-terminal-training admission.

## Final-freeze condition and non-blocking documentation note

Current `HEAD` `47803eaf98aecfe1ccaed232800cc9587c7ec20a` is the explicitly
identified WIP collector commit, not scientific acceptance. The collector now
has one post-commit source-pin-only change adding the guard and guard fixture;
the guard files are also not yet in that commit. Consequently the committed-
source guard correctly fails closed at this WIP HEAD. Main must create and
record a final reviewed commit containing the exact hashes above before any
measurement. This is a required prelaunch freeze, not a defect in the proposed
source.

`measurement-implementation.md` still records the pre-guard adapter hash
`0580e6db...`; the current adapter hash is `a6d01790...` after the two source-pin
entries were added. The runtime source map and final-HEAD guard use the current
full hashes, so this is non-blocking stale provenance prose. Updating it during
the final freeze would improve clarity.

Verdict boundaries: wait for successful terminal acquisition, freeze the
reviewed measurement sources in a new commit, record the actual service/PID/
ExecStart/effective limits and require every real admission check to pass. Do
not bypass the guard, retry a partial output, load any old state for inference,
or proceed on a source, roster, environment, receipt or resource mismatch.
