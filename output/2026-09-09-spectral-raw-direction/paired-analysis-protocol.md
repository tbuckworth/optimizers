# Fixed raw-direction endpoint comparison

9September2026. Specified before new raw-policy acquisition/outcome inspection.
This follows the reviewed discriminator after observing the archived action
results; it is not a claim that the entire research programme is preregistered.

Admit only the completed15-state measurement, its exact roster and bound
raw/scalar/analyzed/checkpoint/source receipts. Read the accepted action
summary at `../2026-09-09-spectral-grokking-action/results/summary.json`, SHA256
`249a8b9f5b680f72f7e9a402f56a9a4734814d99dc7d8097442eac4c965aa237`.
Reuse its archived norm-matched/orthogonal rows and hash-bound archived-native
reference rows. No old inference, new fitting or new frequency selection.

Primary comparison: raw_norm_matched minus archived norm_matched at2500,
also reporting the fixed2000 endpoint. Secondary comparisons: raw minus
archived native and raw minus archived orthogonal, at the same two endpoints.
Every comparison uses allfive seeds100–104; retain the15new rows including1501
without treating the latter as an additional primary endpoint. No within-seed
sample selection, missing-seed favorable subset or endpoint extension.

Reuse the14metric definitions/units/directions in the unchanged
`experiments/analyze_grokking_action_results.py`. Three primaries: held-out CE
(lower), correct-class margin (higher), selected-five final-hidden held-out
probe R² (higher). Readability is not itself task performance. Accuracy remains
a fraction in JSON, percent only when labeled in presentation. All other
metrics remain secondary/descriptive with explicit undefined ratios.

For each metric/comparison/endpoint retain five pairs, five differences, their
arithmetic mean, sample SD, SE=SD/sqrt(5), positive/zero/negative signs and
favorable count. Seeds are units; not frequencies, classes, examples or null
draws. No p-value, composite success metric, equivalence threshold or claim
that a small/non-significant contrast proves equal performance. Missing/nonfinite
primary entries fail admission; secondary undefined ratios stay undefined,
never zero-filled or aggregated over a selectively reduced roster.

All comparisons have an archived-reference limitation. The scale *function*
is the same but its numerical sequence, observer and Adam histories are
branch-local. A new raw-policy deficit supports conditional value of the
direction-restricted continuation including feedback; improvement weakens
its post-fork necessity. Neither identifies effects before1500, a semantic
usefulness criterion, safety or general speed. Known CUDA sensitivity remains
material for small differences. Do not infer causal mediation from correlations
between the logged action/Adam norms and eventual outcomes.