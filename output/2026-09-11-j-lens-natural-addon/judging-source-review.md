# Natural-text add-on judging source freeze

Source preparation only, 11 September 2026. No actual package, reader,
response seal, grade, checker, model, tokenizer or scientific-array stage was
invoked by this task. The completed collection and preflight were not repeated.

MAIN commit `dd4d20074358a9c0cefe3d2a133441cff61d9a64` freezes exactly:

| Source | SHA256 |
|---|---|
| judging.py (artifact not distributed in this public snapshot) | `b11302567336bfed8b7ace1b21734f2144e8c393fab47abb2a2edabe8e6636b8` |
| test_judging.py (artifact not distributed in this public snapshot) | `8b6df4de01e7ef54f90bb732caeedc406c64de8cb76faca6e7429c3e81f3e679` |

The fully read predecessor is MAIN
`output/2026-09-11-jlens-fresh-authored-comparison/judging.py`, SHA
`50de1fff65d4fc50f0aaea0bf901e9096a01fcc62d0c388aba774017d8f942bf`,
and its full test source, SHA
`8c7d1eb8ed3e99c52ef0d10e6503ef259bfeaf03ebafc1d0c26e5eab0cc1e6bf`.
These were adapted by source snapshot, not imported or executed as old stages.
The current protocol and measurement review were read completely; the new
source and tests were then self-reviewed completely. The best-practices skill
guided strict duplicate/nonfinite JSON and no-follow/exclusive file handling;
installed Python 3.12 JSON/open documentation was checked without network I/O.

## Fixed construction and interfaces

E receives the unchanged C example string for each pole. EA receives exactly
that string plus the complete unchanged twelve-element A token list. All
Unicode, whitespace, duplicate and empty token strings survive. Original
reference signs never reverse. Only target sides reverse across cohorts.
Readers 1/3 are homogeneous EA; 2/4 are E, all four axes and sixteen pairs each.

One local Random(20260920) draws 64 orientation flags, then shuffles block IDs,
item IDs, the axis order and each axis's pair order, in that order. Display
orders are shared by all four readers; unique anonymous IDs are consumed in
reader order. Packet IDs use the remaining same RNG stream. E/EA target text
and order match within cohort. No rerolls or global-RNG mutation occur.

The exact collected bare-list N01-L/R through N16-L/R roster is checked against
the saved complete token records: sixteen normalized words, same row and pair
order, category joins, candidate ordering, all-valid masks and complete ordered
offset coverage. Overlapping Unicode-subtoken offsets remain valid. Capture
must be the last actual input token, named prefix_end; no verb truncation.

Release metadata has nine artifacts. Manifest inputs include the seven frozen
protocol/reference/dataset/pair/producer/token/preflight bindings. Producer is
metadata only: packaging does not open producer, forward receipt or scores.
The public-only verify_response_lock signature is unchanged and returns
manifest/public/lock/choices after validating all 256 choices and exactly five
committed lock/response blobs. It does not open any private map or release,
input, producer or scientific file. Grading subsequently reconstructs the
public/private joins, validates the forward receipt and reads only the
singleton prefix_end score export. Every actual stage consumes an exclusive
directory, including failures.

Primary summaries cover EA minus E over all four axes and both cohorts, with
each EA reader compared strictly against its own stronger realized constant
FIRST/SECOND credit. Exact ties receive half credit; tiny nonzero gaps remain.
PC4 is secondary. All items, category counts (including empty categories),
reader/axis/cohort reductions, chosen-text agreement and paired gains/harm
remain accessible. No balanced-category average or independent-text claim.

## Verification and provenance

All seven FROZEN hashes were independently compared with their actual files
using hash-only reads: MAIN protocol and excerpts dataset/pairs; WORKER
original fresh-content references, current forward.py and token-preflight
tokens/receipt. They match the source literals. No scientific scalar contents
were inspected. Main owns future immutable release and prompt/transport pins.

Final fabricated command, from MAIN:

```sh
env CUDA_VISIBLE_DEVICES= OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 timeout 60s /usr/bin/python3 output/2026-09-11-jlens-natural-addon/test_judging.py
```

23 tests PASS in 17.429 seconds. Tests use temporary fabricated JSON/text/Git
repositories only. They cover exact add-on references and matched layouts,
rosters/offsets, strict responses, zero/tiny gaps, all reductions and harm,
consumed paths, byte caps/symlinks, committed release and five-blob lock gates,
and successful public verification after every private/scientific fixture is
deleted. The final assertion checks every FROZEN key in manifest.inputs,
including producer. Main's preceding independent run passed 23 tests in
17.826 seconds before that three-line regression assertion was added.