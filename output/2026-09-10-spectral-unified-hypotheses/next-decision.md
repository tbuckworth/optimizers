# After the unified hypothesis report

Codex — Spectral Optimizer Investigation · 10 September 2026

The [unified report](../../research/spectral_optimizer_unified_hypotheses_2026-09-10.md)
is complete. It supersedes the September 7 broad hypothesis ranking, not raw
results or historical reports. Its offline reader and PDF present visible
mathematics and two unchanged completed-study plots. Do not select another
generic synthesis pass or repeat the completed safety synopsis.

## User clarification changes the next priority

## Next selected work: ordinary augmentation/generalization design

Prepare a clean, balanced all-class training-from-initialization comparison:
raw AdamW versus current stable spectral, each with and without one ordinary
prospectively fixed label-preserving transformation family. Small translations
are a candidate, not yet a frozen recipe. All classes appear from update one;
augmentation is active during observer warmup as well as subsequent filtering.
One view per occurrence unless a distinct later question justifies extra views.
No cue, wrong-label or rare-class factorial is needed for this first question.

A bounded inventory already found no exact matching factorial in the searched
sources; do not repeat that general search. The previous Clean/Random study
applies masks to all images, but only after a common-only warmup in an imbalanced
task. Historical CIFAR uses conversion/normalization; “Augmented MNIST” means
fixed appended Gaussian features. These are not the desired experiment.

Match model initialization, labels, indexed example occurrences, updates and
augmentation draws across optimizer policies. Visually inspect the transform's
label-preservation plausibility. Report held-out CE/accuracy, training progress,
full curves and measured runtime under predeclared endpoint/selection rules.
Show all four absolute outcomes beside the augmentation-by-optimizer interaction:
an interaction can favor spectral merely because raw deteriorates. Fixed-recipe
performance does not establish each optimizer's best attainable performance or
covariance-mediated invariance; augmentation changes the objective and trajectory.

Use three paired fresh seeds at small local MLP scale, CPU/RTX3090, with exact
cells, runtime/memory/output caps, source freeze and analysis established before
execution. No GPU launch is selected by this writing checkpoint. Do not convert
ordinary design work into an unnecessary user-approval gate. No repeated
transform tuning after observing outcomes.

This is a user-prioritized extension, not a requirement for reporting the
current positive contribution. Familiarity-by-frequency, fresh balanced scalar
comparisons, frozen-span grokking, blockwise persistence and blinded J-Lens scoring remain alternatives,
not a queue to run automatically. Broad capability speedruns, paid judges,
harmful-data training, further cue-targeted work, public submission and optimizer-default changes remain
outside this selected step.

## Recovery