# Saved-case and mathematical synthesis review

No new model, archive calculation, old grader, reader, data acquisition or
optimizer experiment. Goal remains active, user scope J-Lens only, budget
spent/reserved $0/$0. The latest natural add-on services are terminal; no restart.

## Independent evidence review

Existing user-authorized Astra `/root/jlens_simple_comparison` first read the
four fixed JSON sources and committed its complete64-case inventory in the
worker at e53494f575bb5ecac44e2d1f7e5b30fb0f212739. File:
`output/2026-09-11-j-lens-usecase-synthesis/case-review.md`, SHA
4a213cf2ab0fab84e98ca9b1201ccb0a32b2bf470b6d81e14c0144d6a37fe51c.
Root read the full inventory, including the32prefixes,96signed tokens,
8raw examples, all11 repeated gains/harms and all18unchanged cases with
any shared group error. No favorable-case filtering was used.

Root independently reconstructed all64 classifications directly from256
saved individual graded rows in analyze.py, checking against the old128
matched comparisons. It did not invoke or import the old grader. Output SHA
dbcbae37be924d368f38340097208048462bb8058b728af597cdde1a6f4d365f.
Counts5/6/1/7/45 and unchanged27all-right/16all-wrong/2group-dependent match
the independent inventory. The source hashes are checked before parsing.

## Algebra and source review

Root checked the published J-Lens Methods/Limitations, the Haufe author poster
(the poster, not a claim to have read the full NeuroImage article), and OpenAI's
primary automated-interpretability repository. Direct links are in the note.
The new argument is an application of standard regression/cross-moment
identities. Pooled direction, conditional population pattern, normalized lens
display, and finite reader usefulness are kept distinct.

Astra independently reviewed the four-state construction and fixed-axis online
cross-scatter recurrence, then the complete draft note and analysis helper.
It ran only fabricated `analyze.py test`, not the actual derived-output command.
It corroborated all9 possible delta classes, toy covariance/slopes,24 input
orders, group patterns and zero-variance guards. The one material wording
clarification was applied before presentation: changing common centering c
affects the intercept only, but changing relative group means can change
pooled covariance. Activation width is now m to avoid confusing a dimension
with the pattern vector d.

## Implementation review

The implementation-review guidance informed bounded JSON parsing, exact source
pins and exclusive derived outputs. Python3.12 official documentation supports
the JSON duplicate/nonfinite hooks and exclusive file modes:
https://docs.python.org/3.12/library/json.html and
https://docs.python.org/3.12/library/functions.html#open.
No dependency upgrade or new model code is needed. Existing matplotlib/Markdown
rendering conventions are reused. No production covariance implementation is
claimed: the stream function exists only to check the mathematical toy.